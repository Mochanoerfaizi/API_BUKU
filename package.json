{
  "name": "book-api",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:init": "npx sequelize init",
    "db:migrate": "node -r dotenv/config node_modules/sequelize-cli/lib/sequelize db:migrate",
    "db:migrate:undo": "node -r dotenv/config node_modules/sequelize-cli/lib/sequelize db:migrate:undo",
    "db:migrate:undo:all": "node -r dotenv/config node_modules/sequelize-cli/lib/sequelize db:migrate:undo:all",
    "migration:generate": "npx sequelize migration:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "google-auth-library": "^10.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "mysql2": "^3.14.1",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.3"
  }
}